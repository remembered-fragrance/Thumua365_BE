#!/bin/sh
# Thay script mặc định của image postgis/postgis — script đó bật PostGIS trong
# schema `public`. Supabase để PostGIS trong schema `extensions`, và migration
# `0001` tự bật nó ở đó. Cố ý không làm gì.
exit 0
